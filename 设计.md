graph TD
    A[配置管理模块] --> B[签到核心模块]
    C[用户数据模块] --> B
    D[订单数据模块] --> B
    B --> E[奖励发放模块]
    B --> F[签到记录模块]
    F --> G[统计查询模块]
    E --> C
    
    flowchart TD
    A[用户点击签到] --> B{平台签到开关是否开启？}
    B -->|否| C[提示：签到功能暂未开放]
    B -->|是| D{今日是否有订单记录？}
    D -->|否（开启订单限制）| E[提示：今日需要存在订单记录才能签到]
    D -->|是| F{余额≥10元？}
    F -->|否| G[提示：余额不足10元，无法签到]
    F -->|是| H{今日签到人数≥10人？}
    H -->|是| I[提示：今日签到名额已满]
    H -->|否| J{用户今日是否已签到？}
    J -->|是| K[提示：今日已签到]
    J -->|否| L[生成随机奖励（0.1-0.2元）]
    L --> M[增加用户余额]
    M --> N[生成签到记录]
    N --> O[更新当日签到人数/奖励总额]
    O --> P[提示：签到成功，获得X元奖励]
    
    flowchart TD
    A[管理员选择查询日期] --> B{该日期是否有签到记录？}
    B -->|否| C[展示：当前日期暂无签到记录]
    B -->|是| D[展示：当日签到总人数、发放奖励总额]
    D --> E[展示：签到用户明细（UID、用户名、奖励金额、签到时间）]